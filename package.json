{
  "name": "fittrack",
  "version": "1.0.0",
  "description": "FitTrack minimal dev server",
  "main": "server.js",
  "scripts": {
    "dev": "cd frontend && npm install && npm run dev"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
